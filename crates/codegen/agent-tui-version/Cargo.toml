[package]
license = "Apache-2.0"
name = "agent-tui-version"
version = "1.0.0"
edition.workspace = true
description = "Lockstepped Agent TUI version."

[dependencies]
semver = { workspace = true }

[lints]
workspace = true
