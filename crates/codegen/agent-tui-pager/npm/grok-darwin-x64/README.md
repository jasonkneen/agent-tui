# @agent-tui-official/grok-darwin-x64

Platform-specific binary for [`@agent-tui-official/grok`](https://www.npmjs.com/package/@agent-tui-official/grok) on darwin-x64.

Do not install this package directly. Install the main package instead:

```sh
npm install -g @agent-tui-official/grok
```

The main package will automatically pull the correct binary for your platform via `optionalDependencies`.
