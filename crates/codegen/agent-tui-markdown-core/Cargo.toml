[package]
license = "Apache-2.0"
name = "agent-tui-markdown-core"
version = "0.1.0"
edition.workspace = true
description = "Headless markdown analysis sharing Grok Build's exact pulldown-cmark config."

[dependencies]
pulldown-cmark = { workspace = true }

[lints]
workspace = true
